package com.bmt.kaleidoscope_chinesefood.integration;

public class KaleidoscopeDollIntegration {
    // Deferred: this integration registers doll-themed contributor blocks/items when the
    // kaleidoscope_doll mod is present. It depends on that mod's Fabric API, which is not
    // bundled, so it is currently a no-op.
    public static void register() {
    }
}
